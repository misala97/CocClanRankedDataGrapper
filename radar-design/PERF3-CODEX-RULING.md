# PERF3 ruling — bounded closure before release

12 September 2026. Codex owns planning; Claude implements.

Reviewed workspace: C:/Users/michi/Desktop/CodingStuff-worktrees/radar-perf3, branch codex/radar-perf3, HEAD 197be30. Base afe1246 carries no application delta from recorded deployed 4221196. Production state is recorded evidence, not freshly verified here.

## Verdict and evidence

Accept the implemented architecture and completed tasks. Return to Claude for PERF3-close, not another architecture/design slice and not deployment yet. Ready reads under sustained producer load, destructive-test safeguards and a coherent release/recovery path remain closure requirements.

Codex inspected the return, current handoff, release proposal, ledger review/measurement sections and relevant implementation. A separate read-only reviewer covered the previously omitted fix-wave changes, including 267811d, 21c3591, 27fb905, 98459d8 and follow-up fec7feb. No additional product blocker was found. This was static review, not another complete branch review or database test run.

Fresh Codex verification: `npx vitest run -c vite.radar.config.ts static/radar/src/board/BoardPage.test.tsx static/radar/src/hub/queries.test.ts` passed 31 tests in two files. No fresh backend or scale benchmark was run. The sequential two-Loop test establishes namespace isolation, not simultaneous execution; correct that evidence label.

Latest Git check still reports HEAD 197be30, with two untracked files newly present: personal_apps/scratchpad/perf3/cleanup_preview.py and preview_perf3.py. Their ownership is not established; preserve them, do not stage/delete/execute them as part of this ruling. Documentation changes made by this ruling are Codex-owned.

## Nine rulings

1. **Freshness:** keep fresh=120 s, refresh target=120 s and hard expiry=600 s for the initial opt-in release. Accept the measured approximately 7% stale observations as a disclosed interim limitation, not a passed strict-freshness criterion. Continue showing actual age and stale state; do not widen the definition of fresh to make the chart green. Do not add a producer or reduce supported filters now. The <=2 s first-missing-result goal remains unmet and explicitly non-blocking under the previously accepted asynchronous contract. Record that under the measured mixed/write workload cold completion reached p95 16.6 s and max 22.8 s, not merely the quiet 5.8 s median. This is a useful staged improvement, not instant results for every new selection.

2. **Deployment readiness and warm demand:** require prewarming/readiness before enabling the shared path on the first release and before serving a new generation on routine deployments. Reject the proposed timeout loop that silently continues after readiness never succeeds. Use a bounded gate with explicit success, timeout and recovery outcomes. Prefer waiting warm keys with actual reader demand over undemanded warm keys, without breaking the existing alternation/fairness between warm and on-demand classes. Preserve a stable first-demand timestamp; polls must not promote a request repeatedly. Add deterministic ordering/starvation tests and a small empty-store check proving the demanded warm key no longer waits solely for hash order. Do not promise that this removes its own build cost.

3. **Ready reads under load:** YES, p95 <=500 ms is an end-to-end ready-response requirement while the producer is doing its normal work, not only while idle. Measure serialized HTTP requests through two independent web processes against already-published keys during ongoing builds and representative writes. Use at least 20 samples per critical US/DE 12h/24h case, report sample count, median/p95/max, cache age and account enrichment. Do not cherry-pick gaps between builds. Include realistic tone/judgment rows in a bounded fixture supplement or separately disclose and measure that path: the large fixture's missing tone work is a lower bound. If the target misses, profile and fix the responsible bounded read-path cost; return a measured failure if it requires a new architectural tradeoff. Do not reopen completed PERF1 investigations.

4. **First request after worker restart:** accept the reported p95 769 ms as a separately reported once-per-worker initialization exception for this release. Keep the steady-state <=500 ms requirement and <=3 s usable-ready-board browser target. Do not add startup cache tricks just to conceal the sample. Test first request versus subsequent requests explicitly; repeated cold behavior is not this exception. Prewarming board results and warming process-local account/helper state are different things.

5. **Discover duplicate:** preserve the backend's exact key and echoed-segment contracts. Make the hub's Discover control request the existing warm default segment spelling, after proving that this corresponds to the same intended company selection. Preserve the visible Discover label, selected-control state and existing incoming `segment=discover` URLs. No global segment sorting/deduplication and no extra permanent warm key. Verify actual membership equivalence at fixed time and test the resulting API request/control state. If equivalence fails, return that discrepancy rather than substituting a different universe.

6. **25 watched companies:** YES, measure the per-account median against its existing 150 ms trigger while the producer is active, alongside the whole ready-response p95 gate. Test 0/3/10/25 watches on the existing critical cases. The 150 ms figure is a diagnostic trigger for the account-enrichment strategy; do not confuse it with a p95 threshold or a watch-count limit. If triggered, implement the already bounded account-aware optimization only after measuring its cause, retaining watch changes, key/as-of identity, freshness and isolation. No account data enters a shared viewer-invariant blob.

7. **Flags, revision and release sequencing:** accept `/root/coc-stats/.env` as the single proposed flag source, subject to confirming actual dotenv precedence and absence of a nearer file. Explicit `off` is the rollback value. Keep full repository revision plus configuration fingerprint; do not replace it with a hand-selected path hash in this release. No persistent manual BUILD_REVISION pin. Rebuild/prewarm on a new revision and record the cost.

   Reject serving old web processes while checkout, dependencies, templates and assets change. The existing shared-checkout deployment must stop BOTH web units before checkout/build/migration, as previously ruled, and stop the producer/affected background processes in the established order. Declare the outage, including prewarm time; do not repeat the old 25-second estimate. Prepare first-rollout and subsequent-rollout paths consistently. Start the new producer after code/schema/build are ready, verify the intended namespace, pass the bounded readiness gate, then start the web services at the correct flag state. No silent mixed-version serving and no silent readiness bypass. Restore service states explicitly after partial failures; a nonzero deploy exit does not prove no services restarted.

   Recommend enabling the already implemented request/board timing telemetry alongside the eventual shared-path activation after verifying logging destination/retention. This is release preparation, not activation authorization. Without it the live acceptance question remains blind. Do not add Gunicorn threading or the held index.

8. **Destructive-test guard:** reject the two-name denylist as sufficient protection. Table existence and a count of unrelated rows do not prove disposability. Require an explicit test opt-in matching the ACTUAL bound host/port/database, checked before any destructive fixture or migration; use a provisioned disposable marker or equivalent explicit target registration as a second check. Preserve the protected-name refusal. Unknown/mismatched targets must fail loudly with setup instructions, not silently skip or label themselves disposable. Do not automatically derive the opt-in from the same potentially wrong connection string. Permit intentionally provisioned clones with baseline data. Apply consistently to the affected suites and destructive benchmark/rehearsal helpers. Prove refusal causes zero writes/DDL and that one registered disposable database runs both migration suites. Do not touch TE1/B1/history or the shared PERF1 fixture to satisfy tests.

9. **Unreviewed commits:** closed by the independent read-only pass in this Codex review. No need to redispatch those five commits as a standalone task. Review the NEW PERF3-close delta after implementation. Correct the sequential namespace test's wording; add a bounded real two-process generation overlap test if it is being used as rollout-concurrency evidence. Do not claim historical tests were concurrent when they were not.

## Release-specific closure requirements

- Reconcile PERF3-RELEASE.md in place: remove now-fixed debounce/focus/guard items from the open-risk list, and qualify all live SHA/configuration statements as recorded until freshly read. Keep one current execution path rather than contradictory historical snippets.
- Prepare exact readiness failure handling. A timeout stops activation and leads to a documented recoverable state; it must not leave unrelated services indefinitely stopped. No blind repetition of update_coc.sh to retrieve output. Write a durable execution log once and inspect it read-only.
- Rehearse interrupted additive migration states on disposable MariaDB: partial table/index creation and all DDL completed before stamp, plus recovery preserving unrelated tables. Gate destructive cleanup on confirmed revision/object state and first-deploy empty-cache conditions; never generalize 'drop both tables' to an established installation. Re-run store rehearsal against the FINAL changed code where SQL/store behavior changed since c199d06. Existing successful full upgrade/downgrade evidence stays accepted; supplement it, do not restart the entire migration program.
- Prefer flag rollback with schema retained. For a full code rollback, prepare/rehearse a compatible rollback commit retaining the new migration file so the routine deploy upgrade can resolve the stamp. Do not make production downgrade the default recovery or run old code against an unresolvable revision. A deliberate downgrade remains a separately documented, unexecuted option with consumers stopped.
- Fresh read-only target checks from the existing release list are within the established read-only scope; do not ask the owner again merely because a new chat started. Do not bypass a tool's approval rejection. If access is unavailable, record exact missing facts and return a conditional package; don't invent unit values or represent live deployment as ready. Inspect only required environment keys safely, never dump secrets. No production configuration changes or service actions during this task.
- Preserve no capture enablement, no root promotion, no B1 merge, no threading and no held c4e17b90d3f2 migration. PERF3 is separable from B1's remaining narrative correction.

## Claude assignment: PERF3-close

Continue in the existing PERF3 worktree after verifying Git and ownership. Write a bounded closure section in PERF3-PLAN.md/LEDGER.md using the rulings above, then implement the guard, demanded-warm ordering and Discover request alignment. Run the loaded ready/account measurements and the missing release recovery rehearsals. Fix measured regressions at their responsible layer. Do not change freshness defaults or grow the scope into another performance architecture.

Run relevant regression suites/build checks for the changes. Reuse previously verified results where unchanged; do not repeat the whole backend suite just to reduce an already classified baseline count. One implementation worker followed by an independent read-only review of the closure delta; resolve findings. Update HANDOFF.md, RELEASE.md and the current evidence summary with exact SHA, files, tests, unmet goals and conditional target facts.

Return a closure report and one concrete deploy proposal. Stopping point: closure requirements proven, or a specifically measured criterion that requires Codex's decision. No merge, push, deploy, production migration/configuration/restart, capture enablement or root-route change is authorized. The next decision after a successful closure is release approval, not another design phase.
