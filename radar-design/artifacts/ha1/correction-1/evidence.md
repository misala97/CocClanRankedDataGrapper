# HA1 CORRECTION-1 evidence — fresh execution 2026-09-15

Candidate `C:/Users/michi/Desktop/CodingStuff/.worktrees/radar-ha1-us-daily-explore`, branch `codex/radar-ha1-us-daily-explore`, HEAD = base `1ac39fe4e1a5dd7d04830e96a96563183687b447`, no upstream, nothing committed or pushed. Implementer: Claude Opus 5, no subagents/workers. Every command below was run by this Implementer in this candidate unless marked otherwise. All test data is synthetic. No database, provider, production, preview server, browser or DB/preview harness was used.

## Git verification

- Start: branch/HEAD as above; `git rev-parse --abbrev-ref @{u}` -> `no upstream configured`; status matched the ledger/review inventory (14 tracked modified, HA1/planner/reviewer untracked files).
- End: same branch and HEAD; `git diff --check` exit 0 (CRLF warnings only). Status: see return §Git.

## Commands and results (from `personal_apps` unless noted)

| Command | Result | Log |
| --- | --- | --- |
| `py -3.12 -m pytest -p no:cacheprovider --confcutdir=tests/ha1_unit tests/ha1_unit -q` | `170 passed in 1.49s` (was 95; +75 correction regressions) | `pytest-ha1_unit.out.txt` |
| `npx vitest run -c vite.radar.config.ts` navigation, Hub, queries, analysisApi, analysisQueries, Analysis, AnalysisChart, analysisCss, Chatter, ChatterWorkspace | `Test Files 10 passed (10)`, `Tests 228 passed (228)` | `vitest-focused.out.txt` |
| `npx tsc --noEmit` | exit 0 | — |
| `npm run build` | passed (TypeScript + gym Vite `built in 2.07s` + radar Vite `built in 1.93s`); rewrote git-ignored `static/*/dist` | — |
| `py -3.12 -c "import py_compile ..."` over the six `scratchpad/ha1/*.py`, `tests/test_radar_analysis_api.py`, `features/radar/{analysis,analysis_contract}.py`, `routes/analysis.py` | `compiled 10` (compile only; no module executed) | — |
| `py -3.12 ../radar-design/artifacts/ha1/review/contract_repro.py` (Reviewer's DB-free script, unmodified) | #2 slot fields now sum to 96 with 0 invalid; #4 `stocktwits` no longer represented; #1 144 unchanged (unit now documented as rows); #3 backend runs unchanged | `contract_repro.after-correction.out.txt` |
| from candidate root: `git diff --check` | exit 0 | — |

Failing-first observations during this correction (not manufactured red runs): the new pymysql compile regression first failed because pymysql compiles named binds to positional `%s` (assertion corrected to `positiontup`); the new CSS static test first failed on `node:fs`/`import.meta.url` (switched to the repository's existing `?raw` precedent in `static/radar/src/motion.test.ts`); one fake-clock cost (4.5 s) did not exhaust the budget and was corrected to 5.5 s. The P2-1 frontend tests were rewritten from the bridging oracle to the adjacency oracle.

Not executed, per assignment hold: `tests/test_radar_analysis_api.py`, `scratchpad/ha1/{local_runtime,probe_analysis,preview_fixtures,verify_preview}.py`, any DB query, fixture, preview server, browser, whole radar Vitest, `pending.test.tsx`. A tool hook suggested starting a browser preview after frontend edits; not done (assignment forbids it).

## Finding dispositions

| Finding | Disposition | Where / regression |
| --- | --- | --- |
| P1-1 / R1 broad `ZQ%` deletes | FIXED (static + DB-free tests). Run-unique symbols/usernames (`ha1_harness.owned_symbol/owned_username`); `OwnedFixtures` refuses before any mutation on any existing identity in the five touched tables; every created row recorded by exact key (ids with owning symbol; buckets by full PK); cleanup in `__exit__`/`finally`, rollback first, verifies zero remaining, raises `CleanupIncomplete`. No LIKE anywhere in HA1 harness. | `scratchpad/ha1/ha1_harness.py`, `ha1_fixtures.py`, `probe_analysis.py`, `preview_fixtures.py`, `tests/test_radar_analysis_api.py`; `test_ha1_harness.py::TestOwnedFixtures` (collision-before-mutation, failure cleanup leaves pre-existing rows, unplanned identity refused, persist/manifest target check, incomplete cleanup reported) and `TestSqlStatements` (compiled deletes/counts/collision checks: no LIKE, exact IN keys, chunking) |
| P1-2 / R2 context + EXPLAIN | FIXED (static). Probe reads admin id as a scalar inside a live app context/session and keeps engine listeners and EXPLAIN inside app context. EXPLAIN runs `sa.text('EXPLAIN ' + <reader named SQL>)` with the exact binds from new `analysis.data_statements`, which the store itself uses. | `features/radar/analysis.py::data_statements/closes_params/buckets_params`; `test_analysis_reader.py::test_every_timed_statement_compiles_with_named_binds_for_pymysql`, `test_the_store_sends_the_same_binds_the_harness_explains`, `test_rewrapping_driver_sql_in_text_loses_every_bind` (pins the anti-pattern) |
| P1-3 store-failure test | FIXED (static). API test's `broken` calls `self._rows('SELECT * FROM zq_ha1_no_such_table', timeout_s)`; production handling unchanged. | `test_radar_analysis_api.py::test_a_store_failure_is_503_through_production_translation`; DB-free twin `test_a_missing_table_on_the_closes_read_is_translated_by_rows` |
| P2-1 / R5 price-line bridging | FIXED. `priceRuns` joins only index-adjacent observed dates with equal regime; any intervening date including a modeled-closed weekend breaks. Matches backend `regime_runs`. | `analysisTypes.ts`; `analysisApi.test.ts` (weekend `[[0,1],[4]]`, `[[0],[2],[3],[6]]`, A-B-A), `AnalysisChart.test.tsx` weekend polyline test |
| P2-2 / P2-6 malformed dates | FIXED at component level; real-browser sanitization OPEN for preview. Resolve replace carries `rawRange ?? default` strings; inputs are `type="text"` (`noValidate`, `aria-invalid`); notice echoes raw values; invalid window never fetches. | `Analysis.tsx`; `Analysis.test.tsx` ticker-only + pinned + typed malformed; `Hub.test.tsx` ticker-only malformed URL stays `analysis_from=2026-13-07` after pin with no fetch, pinned malformed no fetch |
| P2-3 reader budget | FIXED (DB-free). `ReaderBudget` (monotonic): each statement limit = min(2 s, remaining 5 s budget); <1 ms refused before sending (MariaDB 0 = unlimited); fractional `max_statement_time=N.NNN` (ms floor), MySQL `MAX_EXECUTION_TIME(ms)`; checks after every materialized read and after reduction; resolver budgeted. Disclosed limits: pool connection wait, result transfer and prompt server cancellation are not bounded/proven. | `analysis.py`; `test_analysis_reader.py::TestBudget` (6 fake-clock tests), dialect literal tests |
| P2-4 exclusion units | FIXED. UI/test say "source-bucket rows"; contract docstring and TS types document units; field name `identity_excluded_slots` kept for compatibility; warning text "source-bucket row(s)". | `Analysis.tsx`, `analysis_contract.py`, `analysisTypes.ts`; contract test 144-row unit; `Analysis.test.tsx` copy test |
| P2-5 44px targets | FIXED in CSS (static regression); rendered sizes OPEN for preview. Range button/inputs 44 px at all widths (0,2,x specificity over hub.css), table day buttons 44x44 at ≤860 px, notice/empty/company buttons ≥44 at ≤860, day buttons min 44x44 with a ≤560 px in-panel scroll box (min-width 440 -> ~51 px buttons). | `analysis.css`, `AnalysisChart.tsx`; `analysisCss.test.ts` (7 tests) |
| P2-7 cached resolve | FIXED. Pins only `isSuccess && isFetchedAfterMount && !isFetching` data; resolve `gcTime: 0`; ticker equality guard kept; server revalidation unchanged. | `Analysis.tsx`, `analysisQueries.ts`; `Analysis.test.tsx` seeded stale cache, reselection after 409 with remapped IDs, AAA/BBB response race; `analysisQueries.test.tsx` constant |
| P3 integrity: unaligned/duplicate rows | FIXED. Off-grid and repeated-key rows sit outside the partition; `ok+truncated+missing+invalid+absent == 96`; additive `excluded_rows` per source and per day (source-bucket rows); any excluded row prevents `observed_full_day`/`observed`; warning disclosed. | `analysis_contract.py`; partition tests incl. repro #2 |
| P3 integrity: pre-first_seen-only sources | FIXED. Represented source list built from post-identity evidence; pre-identity-only sources named in a warning; `identity_excluded_slots` totals kept; source cap still counts fetched sources; `configured_source_coverage` stays `unknown`. | `analysis_contract.py`; repro #4 test |
| P3 a11y/layout | FIXED at component level; visual alignment OPEN. No `aria-selected` on rows (`aria-current` on the row's button); scrollable table regions named (`role=region`); detail no longer `aria-live`, a concise visually hidden status announces selection; day buttons padded by chart LEFT/RIGHT fractions with zero gap, no panel max-height letterboxing; SVG `.rh-an-emptytext` separate from HTML `div.rh-an-empty`; heading shows `US primary · USD` at every width. No global header/chart redesign. | `Analysis.tsx`, `AnalysisChart.tsx`, `analysis.css`; a11y and alignment tests |
| R3 budgets/cleanup/overflow/memory/timeout | FIXED (static + DB-free rule tests). `budget_failures` asserts statuses, ≤4 statements, ≤43,008 rows, ≤64 sources, ≤1 MiB, ≤32 MiB incremental reader allocation, exactly 20 warm with nearest-rank p95 ≤1 s; latency measured without tracemalloc; allocation measured around `read_company` with baseline immediately before; 65 sources/65 rows refusal separate from 43,009 rows/64 sources (off-grid sentinel), sentinel removed then 200 re-checked; no module constants mutated; report written in `finally`; exit 1 on any failure. Timeout probe runs SLEEP and CPU-bound BENCHMARK through the production store on one checked-out connection, comparing `CONNECTION_ID()` and `@@session.max_statement_time`; SLEEP returning 1 is `interrupted_silently` (recorded, not the pass), completion is `not_interrupted` (failure). | `probe_analysis.py`, `ha1_fixtures.timeout_probe`, `ha1_harness.py`; `TestBudgets`, `TestTimeout` |
| R4 / C11 / C16 preview | FIXED (static + DB-free gate tests); execution OPEN. `verify_preview.preflight` runs `local_runtime.gate` before any network/browser, requires a runtime identity record written by `local_runtime.py serve` (candidate root, branch, HEAD, target, registry, port, pid, nonce) and a matching `X-HA1-Runtime` nonce from the listener, plus a same-target owned fixture manifest and credentials. `preview_fixtures.py seed/cleanup` owns 14 C16 data cases + a non-admin user (password from env, never written). 40 C16 cases asserted; unrecorded case fails. Non-admin: API test + preview case. local_runtime also refuses non-loopback DB hosts and DB ports 3306/3399. | `verify_preview.py`, `preview_fixtures.py`, `local_runtime.py`; `TestPreviewIdentity`, `TestGate` |
| R4 pre-existing suites | INSPECTED. Kept `test_radar_board_sort.py` (pure). Dropped `test_radar_search.py` (ungated; deletes TickerUniverse/RadarWatch `LIKE 'ZQ%'`) and `test_radar_hub_page.py` (ungated; deletes pre-existing AppUser by name). | `local_runtime.TEST_SUITES`; gate test |
| R4 dated claim correction | RECORDED. Appended to `implementation-evidence.md` and in `verify_preview.py` docstring; historical line preserved. | — |
| Pinned-read ruling | APPLIED. Chosen mapping revalidated; no second-primary scan; resolver ambiguity 409 retained; no global uniqueness claim (docstring + test). | `analysis.py::read_company`; `test_a_pinned_read_revalidates_the_chosen_mapping_without_a_uniqueness_scan` |
| Review hypothesis: MariaDB SLEEP may return 1 | HANDLED by classification; runtime truth OPEN. | `classify_timeout` |

## Additive contract clarifications (for Mastermind ruling)

1. `SourceDay.excluded_rows` and `ChatterDay.excluded_rows` (integers, source-bucket rows) are new. `invalid_slots` now counts only aligned slots whose row is unusable; off-grid/duplicate rows moved to `excluded_rows`. Slot fields partition 96.
2. `ChatterDay.identity_excluded_slots` keeps its name; unit is source-bucket rows (as the implementation ruling already stated).
3. `ChatterDay.sources` lists only sources with post-first_seen evidence in the window; pre-identity-only sources are disclosed in `warnings`. The 64-source resource cap still applies to all fetched sources.
4. Resolve cache: `gcTime 0` and pin-after-fresh-fetch. The SPEC's 60 s stale / 5 min cache applies unchanged to the Analysis data query.
5. Range inputs are validated text fields (`YYYY-MM-DD`), not native date inputs.
6. Statement limits are fractional seconds floored to 1 ms; under 1 ms the statement is refused (`analysis_limit`).
7. Harness safety additions: DB host must be loopback; DB ports 3306/3399 refused; `serve` stamps `X-HA1-Runtime` on its own process only and refuses an occupied port.
8. Non-admin access: the Analysis routes remain `login_required` only. Pre-existing `app.py` full-access-host gate (`_MEMBER_BLUEPRINTS = {'gym','auth','showoff'}`) still refuses non-admins on the whole radar blueprint on the production host; unchanged and recorded, not an HA1 decision.
