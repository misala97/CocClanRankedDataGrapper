# Selected-price release preflight — Mastermind ruling

2026-09-15. Preflight COMPLETE / accepted with qualifications. Recommend a single owner-authorized release operation: deploy accepted code initially flags OFF, smoke, then enable charts only and smoke stored fallback. Yahoo remains OFF. This recommendation is NOT release approval; no worker dispatched. Local implementation/review and HA1 remain closed.

Mastermind read attached/repository return, Git facts and Linux probe error evidence; freshly verified local HEAD/origin-main daadf3868caedcb5db858378e919cba68b735f8a and all 50 accepted manifest hashes, zero mismatches at 0b8554d0f73cac233dc5e55ba5e72c3f137841b10eabda2b5ba908801682b2c0. Production facts and probes are Deployer-executed, not rerun by Mastermind. Git ignore-file warning qualifies enumeration. All dirt preserved.

Linux synthetic lifecycle/venv proof and bounded FT MariaDB plan/statement-timeout/recovery proof are sufficient for the proposed charts-only release. Do not generalize one ticker's plan/timing to whole-universe performance, claim gunicorn-worker execution or DB-backed pytest, or claim live Yahoo compatibility. Actual observed topology is two workers; WEB_CONCURRENCY absent, so Unknown stays correct. Schema unchanged. Zero live provider requests.

Yahoo: the preflight reports published automated-collection restrictions and no established permission. Keep new Yahoo acquisition OFF. No legal clearance inferred from existing ingest use. Charts-only improves window/label/chatter alignment using stored quotes/daily closes; it does NOT deliver the planned dense Yahoo intraday feed. Headline remains unchanged. Owner should approve that concrete reduced release scope knowingly.

Linux test-only sitecustomize.__doc__ None defect: defer with explicit record. It occurs in probe_child.py after production main; do not label the failed wrapper a passing end-to-end child. Separate lifecycle and production-output evidence stand as qualified in the return. No production defect or new review cycle demonstrated; fix harness before relying on that isolation test on Linux later.

Deployment plan corrections to carry into any Deployer prompt:
- Post-commit hashing must walk the ACCEPTED manifest's explicit paths; the dirty-file-discovery fingerprint script may return a different file set after staging/commit. Never interpret that as source drift or regenerate away acceptance evidence.
- Freshly check origin/main/production drift immediately before push and wrapper. Do not use stale local main or force push. Stage only the 45 accepted non-generated application/test paths; sanitized continuity documents only if intentionally selected, never blanket artifact staging.
- One approval can cover OFF deployment then charts-ON/Yahoo-OFF activation, conditional on successful smoke. No need for a second owner interruption within that approved scope.
- Verify both flags' effective values, not just grep line counts. Edit only the charts flag; retain private env backup and unrelated settings. On flag rollback restore only owned setting if unrelated env changes occurred.
- Routine wrapper requires fresh recoverable backup and established stop/start order. Historical 106-second outage is an estimate, not a guarantee.
- Rollback through a scoped revert in a safe candidate checkout and normal push/wrapper; do not switch/reset dirty or stale main. Preserve evidence rather than automatically reverting documentation. No schema downgrade.
- Stored fallback may be missing or sparse for some tickers; smoke validates honest states and aligned payload, not fixed row counts at a later time.

Next owner decision: commit/push accepted code, backup-first routine deployment and charts-only activation (Yahoo OFF), smoke and necessary rollback. No action yet. No repeat preflight except fresh drift/health/backup checks at release. Candidate C:/Users/michi/Desktop/CodingStuff/.worktrees/radar-selected-price-charts, codex/radar-selected-price-charts. This assessment owns ruling/current notices only; source/other worktrees/production/RC untouched.