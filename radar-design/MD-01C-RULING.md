# MD-01C acceptance and planning ruling

2026-09-14. Mastermind only. Accept MD-01C-RETURN.md as sufficient Researcher-reported evidence to prepare the first implementation packet. Owner authorized direct bounded VPS reads; earlier snapshot/restore proposals are superseded. No repeat measurement or independent production probe is required merely for acceptance.

## Evidence and limits

Local branch codex/radar-hub-promotion and HEAD/local origin/main verified at 1ac39fe4e1a5dd7d04830e96a96563183687b447. Existing documentation dirt and untracked reports/preview artifacts preserved. Report contains query templates, summaries, timestamps and durations; not a complete raw execution transcript. Production measurements remain attributed to Researcher, not independently reproduced by Mastermind.

Price denominators reconcile: 305 zero + 219 one + 12,075 two-or-more = 12,599; any usable = 12,294. Chatter availability reconciles: 8,037 + 4,562 = 12,599. Current catalogue is US-mapped; 100% US eligibility does not demonstrate global company coverage or prove German/international discovery is comprehensive. Seven calendar days/four peer dates establish a useful starting window, not months of validated depth. No DE daily rescue in that window supports this scope but not source retirement. Nonempty source/adjustment metadata is not proof of economic comparability through corporate actions.

One reproducibility defect: the generic Q4 displayed in the report sums u.close_date IS NULL after an outer join. With two adjacent closes and no interior peer dates, its synthetic NULL row can count one false gap. Researcher describes separately executed bounded stratum queries, but their complete SQL/output is not included; the reported 195 instruments/225 gaps is therefore provisional. Preserve this qualification; do not use it as a correctness oracle. When building the gap contract, count only non-null peer dates with missing closes and test adjacent two-close/no-interior-date cases. Request the exact already-executed query only if that statistic becomes decision-critical; no production rerun needed now.

Report templates also omit full stratum predicates and raw per-source/config output. Treat headline results as attributed evidence. Q7 bucket timestamps do not freeze later bucket revisions; this is retrospective live-state evidence. Retained mention counts measure count availability, not validated historical z-score aggregation.

## Approved preparation scope

Prepare HA1-US-Daily-Explore as one authenticated selected-company/instrument path in the actual dark Radar hub. Reuse existing native-USD US-primary daily closes and durable per-source chatter buckets. Default bounded seven-day window is supported by the measurement; wider supported ranges require selected-instrument query/availability evidence during scoped implementation, not an assumption from total table size.

Show independent price/chatter availability, missing vs observed zero, truncated/config-transition coverage, source/venue/currency/adjustment identity, and a visible retrospective label. Empty/one-close cases remain usable explanatory states. Chart counts are mentions; do not sum/average bucket mention_z into a daily score. Human Chatter's current price-independent mention_z-before-top-N policy remains unchanged. Any new historical unusual-activity series requires an explicit statistical contract and is outside the minimal counts-and-price slice.

Daily gap treatment must distinguish missing expected observations from holidays/weekends; peer evidence cannot silently become an authoritative exchange calendar. Price-series regime changes must not be spliced into unexplained returns. No requirement to implement replay, studies, full population capture, durable tone, provider changes or collection activation in this first slice. Preserve international/German behavior. Actual-app desktop/mobile acceptance; no separate interactive prototype and no redesigning-pages-from-data.

OpenTerminal remains active: MD-02/07 selected-instrument intraday and independent series resolution, MD-05 quote priority, and MD-10 bounded cache/coalescing/timeouts/backoff/health form the next enabling packet. MD-03/08 broad US/German observations remain separate provider evidence work. Do not imply the daily slice implements these capabilities or postpone them behind portfolio/news. Existing-data endpoints still require bounded efficient reads and honest errors now; only new-adapter controls wait for the adapter.

## Status and next step

Mastermind owns preparation of the binding implementation spec/plan and acceptance criteria. No separate planner automatically dispatched; owner selects workers/models. This ruling authorizes planning only, not application code, migration, provider contact, capture, push or deployment. Original Researcher report remains unchanged. All new continuity is local/uncommitted.
